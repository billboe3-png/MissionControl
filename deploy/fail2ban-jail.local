[DEFAULT]
bantime = 1h
findtime = 10m
maxretry = 5
ignoreip = 127.0.0.1/8 ::1

[sshd]
enabled = true
port = ssh
filter = sshd
logpath = %(sshd_log)s
banaction = geoip[name=SSH, port=ssh, protocol=tcp]
banaction_allports = geoip[name=SSH, port=all, protocol=tcp]
